// 1)Создать массив. Получить последний элемент массива. 
// Без удаления этого элемента из массива. С удалением этого элемента из массива. 
// Выведите массивы в консоль

let last = [1, 2, 3, 4, 5];
console.log(last.at(-1));
console.log(last);
console.log(last.pop());
console.log(last);